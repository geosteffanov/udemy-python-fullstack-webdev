console.log("Main connected.");

createTable();

// changeColor("cell_05", "blue");

addEventListenersToCells();

//gather player info

var player_one_id = prompt("Player one, what is your id?");
var player_two_id = prompt("Player two, what is your id?");
var players = [player_one_id, player_two_id];

var currentColor = "blue";
var currentPlayer = player_one_id;

// keep track of chips on rows

// prompt player

// main loop

